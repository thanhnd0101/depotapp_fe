import { ReactElement } from 'react';
import { DefaultTheme } from 'styled-components';
import { CommonProps } from '../common';
interface ProgressCircleProps extends CommonProps {
    intent: 'primary' | 'danger' | 'success';
    size: 'small' | 'medium';
    value: number;
    withInfo: boolean;
}
export declare const mapColorFromIntent: (intent: ProgressCircleProps['intent'], theme: DefaultTheme) => string;
export declare const mapStrokeWidthFromSize: (size: ProgressCircleProps['size'], theme: DefaultTheme) => string;
declare const ProgressCircle: ({ intent, size, withInfo, value, id, className, style, "data-test-id": dataTestId, }: ProgressCircleProps) => ReactElement;
export default ProgressCircle;
