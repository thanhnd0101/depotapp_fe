import { ReactElement } from 'react';
import { CommonProps } from '../common';
export interface ProgressProps extends CommonProps {
    /**
     * Size of progress.
     */
    size?: 'small' | 'medium';
    /**
     * Status of progress.
     */
    status?: 'active' | 'exception';
    /**
     * The progress completion percentage: 0-100.
     */
    value: number;
    /**
     * Progress type.
     */
    variant?: 'bar' | 'circle';
    /**
     * Whether to display the progress value and the status icon.
     */
    withInfo?: boolean;
}
declare const Progress: ({ size, withInfo, status, value, variant, id, className, style, "data-test-id": dataTestId, }: ProgressProps) => ReactElement;
export default Progress;
