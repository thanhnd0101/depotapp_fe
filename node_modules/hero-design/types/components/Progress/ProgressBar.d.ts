import { ReactElement } from 'react';
import { CommonProps } from '../common';
export interface ProgressBarProps extends CommonProps {
    intent: 'primary' | 'danger' | 'success';
    size: 'small' | 'medium';
    value: number;
    withInfo: boolean;
}
declare const ProgressBar: ({ intent, size, withInfo, value, id, className, style, "data-test-id": dataTestId, }: ProgressBarProps) => ReactElement;
export default ProgressBar;
