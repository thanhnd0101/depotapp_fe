import { ReactElement, ReactNode } from 'react';
import { CommonProps } from '../common';
export interface PageHeaderProps extends CommonProps {
    /**
     * Breadcrumbs to display.
     */
    breadcrumbItems?: {
        href: string;
        text: string;
        withRouter?: boolean;
    }[];
    /**
     * Title text.
     */
    title: ReactNode;
}
declare const PageHeader: ({ breadcrumbItems, title, id, className, style, "data-test-id": dataTestId, }: PageHeaderProps) => ReactElement;
export default PageHeader;
