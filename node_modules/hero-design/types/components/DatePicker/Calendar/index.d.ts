import React, { ReactElement } from 'react';
interface CalendarProps {
    maxDate?: Date;
    minDate?: Date;
    onSelectDate?: (date: Date) => void;
    selectedDate?: Date;
}
declare const Calendar: {
    ({ selectedDate, onSelectDate, minDate, maxDate, }: CalendarProps): ReactElement;
    Double: ({ startDate, endDate, minDate, maxDate, onSelectDate, isSettingStartDate, isSettingEndDate, dateClickCount, }: import("./DoubleCalendar").DoubleCalendarProps) => React.ReactElement<any, string | React.JSXElementConstructor<any>>;
    Month: ({ selectedMonth, onSelectMonth, minDate, maxDate, }: import("./MonthCalendar").MonthCalendarProps) => React.ReactElement<any, string | React.JSXElementConstructor<any>>;
};
export default Calendar;
