import { ReactElement } from 'react';
import { CommonProps } from '../common';
export interface SelectButtonGroupProps extends CommonProps {
    /**
     * Name of the select button group.
     */
    name: string;
    /**
     * Change event handler receiving selected value.
     */
    onChange: (value: string | number) => void;
    /**
     * An array of button options to be selected.
     */
    options: {
        disabled?: boolean;
        text: string;
        value: string | number;
    }[];
    /**
     * The size of the select button.
     */
    size?: 'small' | 'medium' | 'large';
    /**
     * Value currently selected.
     */
    value?: string | number;
    /**
     * Button type.
     */
    variant?: 'filled' | 'outlined';
}
declare const SelectButtonGroup: ({ name, value, options, onChange, size, variant, id, className, style, "data-test-id": dataTestId, }: SelectButtonGroupProps) => ReactElement;
export default SelectButtonGroup;
