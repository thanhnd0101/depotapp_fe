import { ReactElement } from 'react';
import { CommonProps } from '../common';
export interface StepsProps extends CommonProps {
    /**
     * Current step. This is 1-based indexing.
     */
    current: number;
    /**
     * Set the handler to handle `click` event.
     */
    onClick?: (step: {
        id: string | number;
        status: 'complete' | 'incomplete';
        text: string;
    }) => void;
    /**
     * An array of steps to be rendered.
     */
    steps: {
        id: string | number;
        status: 'complete' | 'incomplete';
        text: string;
    }[];
}
declare const Steps: ({ steps, current, onClick, id, className, style, "data-test-id": dataTestId, }: StepsProps) => ReactElement;
export default Steps;
