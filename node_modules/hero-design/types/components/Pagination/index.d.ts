import { ReactElement } from 'react';
import { CommonProps } from '../common';
export interface PaginationProps extends CommonProps {
    /**
     * Current selected page. This is 1-based indexing.
     */
    current: number;
    /**
     * Change event handler receiving new selected page.
     */
    onChange: (selectedPage: number) => void;
    /**
     * Total number of pages.
     */
    total: number;
}
declare const Pagination: ({ current, total, onChange, id, className, style, "data-test-id": dataTestId, }: PaginationProps) => ReactElement;
export default Pagination;
