import { ReactElement } from 'react';
import { CommonProps } from '../common';
export interface CardContentProps extends CommonProps {
    /**
     * Custom content.
     */
    children?: string | ReactElement;
    /**
     * Short hand for Card's content.
     */
    content?: string | ReactElement;
    /**
     * Format as extra content.
     */
    extra?: boolean;
}
declare const CardContent: ({ content, extra, children, id, className, style, "data-test-id": dataTestId, }: CardContentProps) => ReactElement;
export default CardContent;
