import { ReactElement } from 'react';
import { CommonProps } from '../common';
export interface CheckboxGroupProps extends CommonProps {
    /**
     * Layout to render checkboxes.
     */
    layout?: 'vertical' | 'horizontal';
    /**
     * Checkbox group name, used for form submission.
     */
    name?: string;
    /**
     * Change event handler.
     */
    onChange: (value: (string | number)[]) => void;
    /**
     * An array of checkbox options to be selected.
     */
    options: {
        disabled?: boolean;
        text: string;
        value: string | number;
    }[];
    /**
     * Selected value.
     */
    value: (string | number)[];
}
declare const CheckboxGroup: ({ name, value, options, onChange, layout, id, className, style, "data-test-id": dataTestId, }: CheckboxGroupProps) => ReactElement;
export default CheckboxGroup;
