import { ReactNode, ReactElement } from 'react';
import { Breakpoint } from '../../utils/responsiveBreakpoints';
interface SortingProps {
    sortable?: boolean;
    sorted?: boolean;
    sortedDesc?: boolean;
    toggleSortBy?: (descending?: boolean, multi?: boolean) => void;
}
interface TableTHProps {
    align?: 'left' | 'right';
    children: ReactNode;
    'data-test-id'?: string;
    displayAtBreakpoint?: Breakpoint;
    filterable: boolean;
    hasFilterInput: boolean;
    renderFilter: (props: {
        onClose: () => void;
    }) => ReactNode;
    sortingProps: SortingProps;
    sticky: boolean;
}
declare const TableTH: ({ sortingProps, sticky, displayAtBreakpoint, filterable, renderFilter, hasFilterInput, align, children, "data-test-id": dataTestId, ...otherTHProps }: TableTHProps) => ReactElement;
export default TableTH;
