import React from 'react';

export interface CardContextType {
  size: 'small' | 'medium';
  variant:
    | 'basic'
    | 'primary'
    | 'info'
    | 'primary-dark'
    | 'grey'
    | 'primary-neutral';
}

const CardContext = React.createContext({} as CardContextType);

export default CardContext;
