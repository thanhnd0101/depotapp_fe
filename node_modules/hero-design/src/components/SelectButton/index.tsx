import { FunctionComponent } from 'react';
import SelectButton, { SelectButtonProps } from './SelectButton';
import SelectButtonGroup from './SelectButtonGroup';

interface CompoundSelectButtonProps
  extends FunctionComponent<SelectButtonProps> {
  Group: typeof SelectButtonGroup;
}

const CompoundSelectButton = SelectButton as CompoundSelectButtonProps;

CompoundSelectButton.Group = SelectButtonGroup;

export default CompoundSelectButton;
