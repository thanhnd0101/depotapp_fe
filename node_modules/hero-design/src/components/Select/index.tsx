import { FunctionComponent } from 'react';
import Select, { SelectProps } from './SingleSelect';
import MultiSelect from './MultiSelect';

interface CompoundSelectProps extends FunctionComponent<SelectProps> {
  Multi: typeof MultiSelect;
}

const CompoundSelect = Select as CompoundSelectProps;

CompoundSelect.Multi = MultiSelect;

export default CompoundSelect;
