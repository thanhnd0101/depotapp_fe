import styled from 'styled-components';

import { space } from '../../../theme/global/space';
import StyledRow from '../Row/StyledRow';

type Space = typeof space;
type Gutter = keyof Space | 'none';

const StyledGrid = styled.div<{
  gutter: Gutter;
}>`
  display: flex;
  flex-direction: column;
  padding: 0;
  margin: 0;
  ${({ theme, gutter }) =>
    gutter === 'none'
      ? ``
      : `
          margin-top: -${theme.space[gutter] / 2}px;
          margin-bottom: -${theme.space[gutter] / 2}px;
          > ${StyledRow} {
            padding-top: ${theme.space[gutter] / 2}px;
            padding-bottom: ${theme.space[gutter] / 2}px;
          }
        `};
`;

export default StyledGrid;
