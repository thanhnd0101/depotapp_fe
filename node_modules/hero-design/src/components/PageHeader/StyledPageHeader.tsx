import styled from 'styled-components';

export const PageHeaderWrapper = styled.div`
  margin: 0;
  padding: 0;
`;

export const TitleWrapper = styled.div`
  margin: ${({ theme }) => theme.space.pageHeader.titleMargin};
  padding: 0;
  font-size: ${({ theme }) => theme.fontSizes.pageHeader.title};
  font-weight: ${({ theme }) => theme.fontWeights.pageHeader.title};
  color: ${({ theme }) => theme.colors.pageHeader.title};
`;
