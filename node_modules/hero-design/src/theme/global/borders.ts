interface BorderWidths {
  base: number;
}

interface Radii {
  base: number;
}

const BASE_WIDTH = 1;

const BASE_RADIUS = 4;

const borderWidths: BorderWidths = {
  base: BASE_WIDTH,
};

const radii: Radii = {
  base: BASE_RADIUS,
};

export { borderWidths, radii, BorderWidths, Radii };
