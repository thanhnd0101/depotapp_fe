import { sizes } from '../global/sizes';
import { fontSizes, lineHeights, fontWeights } from '../global/typography';
import { radii } from '../global/borders';
import { space } from '../global/space';
import { systemPalette } from '../global/colors';

const tooltipSizes = {
  arrow: `${sizes.small}px`,
};

const tooltipColors = {
  background: systemPalette.defaultDarkBackground,
};

const tooltipSpace = {
  wrapperPadding: `${space.xsmall}px ${space.small}px`,
  margin: space.small,
};

const tooltipFontSizes = {
  default: `${fontSizes.small}px`,
};

const tooltipFontWeights = {
  default: fontWeights.regular,
};

const tooltipLineHeights = {
  default: `${lineHeights.small}px`,
};

const tooltipRadii = {
  default: `${radii.base}px`,
};

export {
  tooltipSizes,
  tooltipColors,
  tooltipSpace,
  tooltipFontSizes,
  tooltipFontWeights,
  tooltipLineHeights,
  tooltipRadii,
};
