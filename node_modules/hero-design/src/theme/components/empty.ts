import { fontSizes, lineHeights } from '../global/typography';
import { space } from '../global/space';
import { systemPalette } from '../global/colors';

const emptyFontSizes = {
  text: `${fontSizes.medium}px`,
};

const emptySizes = {
  wrapperSmallMinWidth: '300px',
  wrapperSmallMaxWidth: '400px',
  wrapperMediumMinWidth: '400px',
  wrapperMediumMaxWidth: '600px',
};

const emptyColors = {
  text: systemPalette.subduedText,
};

const emptySpace = {
  wrapperPadding: `${space.medium}px`,
  textPadding: `${space.medium}px`,
  extraMarginTop: `${space.medium}px`,
};

const emptyLineHeights = {
  text: `${lineHeights.medium}px`,
};

export {
  emptyFontSizes,
  emptyColors,
  emptySpace,
  emptyLineHeights,
  emptySizes,
};
