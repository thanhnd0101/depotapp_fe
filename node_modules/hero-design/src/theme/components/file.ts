import { radii } from '../global/borders';
import { space } from '../global/space';
import { systemPalette } from '../global/colors';

const fileSizes = {
  dragAndDropMinHeight: '144px',
};

const fileSpace = {
  dragAndDropPadding: `${space.xlarge}px ${space.xlarge}px`,
};

const fileRadii = {
  dragAndDrop: `${radii.base}px`,
};

const fileColors = {
  dragAndDropBackground: systemPalette.defaultBackground,
  dragAndDropBorder: systemPalette.defaultBorder,
  dragAndDropBorderHover: systemPalette.primary,
};

export { fileSpace, fileRadii, fileColors, fileSizes };
