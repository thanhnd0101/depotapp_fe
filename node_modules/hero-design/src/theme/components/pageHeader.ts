import { fontSizes, fontWeights } from '../global/typography';
import { space } from '../global/space';
import { systemPalette } from '../global/colors';

const pageHeaderSpace = {
  titleMargin: `${space.large}px 0`,
};
const pageHeaderColors = {
  title: systemPalette.text,
};

const pageHeaderFontSizes = {
  title: `${fontSizes.xxxxlarge}px`,
};

const pageHeaderFontWeights = {
  title: fontWeights.semiBold,
};

export {
  pageHeaderSpace,
  pageHeaderColors,
  pageHeaderFontSizes,
  pageHeaderFontWeights,
};
