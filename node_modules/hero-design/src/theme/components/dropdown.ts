import { space } from '../global/space';
import { radii } from '../global/borders';
import { shadows } from '../global/shadows';
import { systemPalette } from '../global/colors';

const dropdownColors = {
  background: systemPalette.defaultLightBackground,
};

const dropdownSpace = {
  margin: space.xsmall,
};

const dropdownRadii = {
  default: `${radii.base}px`,
};

const dropdownShadows = {
  default: shadows.default,
};

export { dropdownColors, dropdownSpace, dropdownRadii, dropdownShadows };
